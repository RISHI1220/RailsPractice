<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, show_post_path(article.id) %>
    </li>
  <% end %>
</ul>


<%= link_to "New Article", new_path %>